name: Build and Publish Production

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
    - name: No Operation Step
      run: echo "This is a noop step. No operations performed."
